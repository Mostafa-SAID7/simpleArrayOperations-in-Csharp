{
    "configurations": [
      {
        "type": "coreclr",
        "request": "launch",
        "name": "Launch .NET Program",
        "program": "${workspaceFolder}/${input:programPath}",
        "preLaunchTask": "build"
      }
    ],
    "inputs": [
      {
        "type": "promptString",
        "id": "programPath",
        "description": "Enter the path to your .NET executable (e.g., bin/Debug/net9.0/array.dll)"
      }
    ]
  }